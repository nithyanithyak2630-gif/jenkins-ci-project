# Jenkins Demo Project

This is a simple Continuous Integration (CI) demo project using Jenkins.

## Project Overview
The aim of this project is to connect Jenkins with GitHub and automatically run a build whenever code is updated.

## Files Included
- `test.sh` — A shell script that prints a message to verify Jenkins builds are working.

## Build Steps
1. Jenkins pulls this repository.
2. Jenkins runs the `test.sh` file.
3. Output is shown in the Console Output panel.

## Expected Output
